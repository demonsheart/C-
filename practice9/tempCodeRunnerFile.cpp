if(flag==0)
            // {
            //     p[no] = new Goods(n, name, color, size, price, num);
            //     no++;
            //     total_num++;
            //     total_price += price * num;
            // }